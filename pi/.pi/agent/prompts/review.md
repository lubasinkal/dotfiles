---
description: Review staged + unstaged changes
---
Review all current changes using the `diff-hunks` tool: first with `staged: true`, then the working tree (default). Check for: bugs, type errors, dead code, incomplete cleanup, security gaps.
