---
description: Bootstrap a fresh machine / new repo
---
Ensure git identity: `gh auth setup-git` (user: lubasinkal) — never prompt for name/email. Repo-local identity must match history: nvim submodule = lubasiverse <admin@lubasiverse.com>, dotfiles = lubasinkal <lubasi@users.noreply.github.com>. Package manager: npm or bun, never pnpm.
