---
description: Full proactive security audit
---
Run a complete security audit: auth/trust boundaries, rate limiting, 2FA, injection, XSS, CSRF, dependency audits, deployment hygiene. Verify every claim in code before reporting — never reassure. Default to fixing all code-level issues found.
